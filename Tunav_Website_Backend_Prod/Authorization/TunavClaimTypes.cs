namespace tunav_backend.Authorization;

public static class TunavClaimTypes
{
    /// <summary>JWT claim: one claim per permission code assigned to the user's role.</summary>
    public const string Permission = "permission";
}
